post:可以传输各种各样的数据


Access-Control-Allow-Origin

访问 控制 允许 源点

跨域本身不允许前台控制，允许后台控制

必须在一个域名下

后台控制

iframe

Ajax状态码：

ajax readyState

0 - (未初始化) 还没有调用send()方法

1 - (载入) 已调用send()方法，正在发送请求

2 - (载入完成) send()方法载入完成，已收到响应内容

3 - (交互) 正在解析响应内容

4 - (完成) 响应内容解析完成,可以在客户端调用

服务器状态码：

404 not found 

200 成功


querystring.parse可以解析post数据

url -- 解析get内容

querystring.parse(urlLib.parse(data).query)

urlLib.parse(data,true)

query   json

fs      files   文档文件模块

增：

删：

改：

查：